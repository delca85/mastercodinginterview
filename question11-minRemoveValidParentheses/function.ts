export function minRemoveToMakeValid(s: string): string {
    const stack = [],
        result = [];
    for (let p = 0; p < s.length; p++) {
        if (s[p] === '(') {
        }
    }

    return s;
}
